<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="adminSqls">

<select id="getmemberlist" resultType="com.kh.finalPJ.member.memberDto">
	SELECT * FROM MEMBER
	WHERE NOT AUTH=1
</select>
 
<select id="secessionlist" resultType="com.kh.finalPJ.member.secessionDto">
	SELECT * FROM SECESSION
</select>
<!--
<select id="goodslist" resultType="com.kh.finalPJ.goods.goodsDto">
	SELECT * FROM GOODS
</select>

<INSERT id="goodsRegist" parameterType="com.kh.finalPJ.goods.goodsDto">
	INSERT INTO GOODS VALUES(#{},#{},#{},#{})
</INSERT>

<UPDATE id="goodsUpdate" parameterType="com.kh.finalPJ.goods.goodsDto">
	UPDATE GOODS SET =#{}, =#{}, =#{}, =#{}
</UPDATE>
 -->
</mapper>